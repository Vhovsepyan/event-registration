# Task 0011 Prompt Evidence

The plan requires external email to stay outside business transactions, a PostgreSQL notification outbox with dedupe keys and retry attempts, a polling worker, and Mailpit as the local inspectable SMTP service.
