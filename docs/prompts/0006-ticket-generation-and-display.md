# Task 0006 Prompt Evidence

The implementation plan requires unique, unpredictable, manually typeable ticket codes, active tickets only for confirmed registrations, and a ticket lookup endpoint. This task implements those requirements without entering cancellation or check-in scope.
