# Task 0016 prompt evidence

Source: `docs/IMPLEMENTATION_PLAN.md`, sections 6, 8, 10, 17, 19, 23, and 24, plus the user's autonomous continuation instruction.

Prove the existing database-backed concurrency guarantees and automate the documented two-client flow when locally feasible. Browser A must keep an organizer SSE dashboard open while browser B checks in a real issued ticket, and browser A must update without refresh. Do not replace PostgreSQL tests with mocks or alter the established locking/SSE architecture.
