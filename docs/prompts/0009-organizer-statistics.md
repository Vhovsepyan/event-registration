# Task 0009 Prompt Evidence

The plan requires organizer capacity, confirmed, waitlist, and checked-in values calculated from authoritative registration/ticket state rather than duplicated counters. Task 0009 implements only the snapshot endpoint; SSE follows in task 0010.
